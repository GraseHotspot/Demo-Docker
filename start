/etc/init.d/apache2 start
echo "Waiting for MySQL container to start"
sleep 2
for try in $(seq 1 30)
do
  echo Try $try
  mysql -h mysql -u grase -pgrase -e"quit" && break
  sleep 5
done
#wget -q http://127.0.0.1/grase/radmin/cron.php -O -
#mysql -u root -pgrasesql radmin < /root/demo.sql
#echo "create database radius;" | mysql -u root -pgrasesql radmin
#mysql -u root -pgrasesql radius < /root/overflowdata.sql
su -u www-data /usr/share/grase/symfony4/bin/console doctrine:migrations:migrate --no-interaction
su -u www-data /usr/share/grase/symfony4/bin/console grase:settings-validate
su -u www-data /usr/share/grase/symfony4/bin/console grase:demo
su -u www-data /usr/share/grase/symfony4/bin/console cache:warmup
echo "Entering sleep loop. Webserver should be running"
sleep infinity
